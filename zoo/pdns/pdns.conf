# PowerDNS Auth behind dnsdist: pdns cannot speak dnstap, so it only
# listens locally; dnsdist is the public face.
launch=bind
bind-config=/etc/powerdns/named.conf
local-address=127.0.0.1
local-port=5300
security-poll-suffix=
